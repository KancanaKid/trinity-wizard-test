<resources>
    <!-- Base application theme. -->
    <style name="Theme.TrinityWizardTest" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryVariant">@color/white_color</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/primary_color</item>
        <!-- Customize your theme here. -->
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.TrinityWizardTest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="ToolbarTitle">
        <item name="android:textAppearance">?attr/textAppearanceHeadline6</item>
        <item name="titleTextAppearance">?attr/textAppearanceHeadline6</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
        <item name="android:elevation">4dp</item>
    </style>

    <style name="TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox.Dense">
        <item name="errorIconDrawable">@null</item>"
        <item name="boxStrokeColor">@color/color_input_strokes</item>
        <item name="hintTextColor">@color/color_input_strokes</item>
        <item name="materialThemeOverlay">@style/TextInputEditText</item>
    </style>

    <style name="TextInputEditText" parent="">
        <item name="textAppearanceSubtitle1">?textAppearanceBody1</item>
    </style>
</resources>